# --- serving ---------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27

# --- providers -------------------------------------------------------------
anthropic>=0.40
openai>=1.55            # Groq / OpenRouter / Gemini / vLLM / Ollama (all OpenAI-compatible)

# --- RAG -------------------------------------------------------------------
chromadb>=0.5.5         # vector DB + bundled onnxruntime all-MiniLM-L6-v2 embeddings
pypdf>=4.3

# --- intent router (ONNX serving, no torch) --------------------------------
onnxruntime>=1.18
tokenizers>=0.20

# --- reliability / perf ----------------------------------------------------
tenacity>=9.0
cachetools>=5.4

# --- UI --------------------------------------------------------------------
streamlit>=1.37
